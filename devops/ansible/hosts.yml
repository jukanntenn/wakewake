---
all:
  children:
    dev:
      hosts:
        fn:
          ansible_user: fn
          ansible_host: 192.168.5.200
          ansible_python_interpreter: /usr/bin/python3.11
    # WoL agent 部署目标：与目标设备同网段的 bare-metal 主机。
    # WoL 受限广播（255.255.255.255）不跨越路由器（RFC 919），agent 必须与设备同一 L2 网段。
    dev_agent:
      hosts:
        agent:
          ansible_user: alice
          ansible_host: 192.168.5.58
